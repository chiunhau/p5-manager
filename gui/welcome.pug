extends ./layout.pug

block content
  .wrapper
    h1 Hello p5.js!
    p.magic Your creativity times p5-manager's magic
    h3.started Getting started
    ol
      li Use<code>$ p5 generate PROJECT_NAME</code>to start a new project
      li Run<code>$ p5 server</code>in the collection directory
      li Go to<code>localhost:5555</code>and you should see this page though
      li All your tracked projects would be listed in the left ♪( ´▽｀)

